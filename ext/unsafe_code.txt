En archivo ../proyectos_para_analisar/tokio-master/benches/signal.rs: 
unsafe {
        assert_eq!(kill(getpid(), signal), 0);
    } 
 
En archivo ../proyectos_para_analisar/tokio-master/examples/tinyhttp.rs: 
unsafe { String::from_utf8_unchecked(Vec::from(s.as_ref())) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/fs/file.rs: 
unsafe {
            std::os::unix::io::BorrowedFd::borrow_raw(std::os::unix::io::AsRawFd::as_raw_fd(self))
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/fs/file.rs: 
unsafe {
                BorrowedHandle::borrow_raw(
                    AsRawHandle::as_raw_handle(self),
                )
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/future/maybe_done.rs: 
unsafe {
                *self.get_unchecked_mut().thing += ", world";
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/future/maybe_done.rs: 
unsafe { Pin::new_unchecked(&mut fut) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/async_fd.rs: 
unsafe { std::os::unix::io::BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/blocking.rs: 
unsafe {
            self.buf.set_len(len);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/read_buf.rs: 
unsafe { slice_to_uninit_mut(buf) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/read_buf.rs: 
unsafe { slice_assume_init(slice) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/read_buf.rs: 
unsafe { slice_assume_init_mut(slice) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/read_buf.rs: 
unsafe { ReadBuf::uninit(&mut self.unfilled_mut()[..max]) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/read_buf.rs: 
unsafe { slice_assume_init(slice) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/read_buf.rs: 
unsafe { slice_assume_init_mut(slice) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/read_buf.rs: 
unsafe {
                self.buf[self.initialized..end]
                    .as_mut_ptr()
                    .write_bytes(0, end - self.initialized);
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/read_buf.rs: 
unsafe { slice_assume_init_mut(slice) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/read_buf.rs: 
unsafe {
            self.buf[self.filled..end]
                .as_mut_ptr()
                .cast::<u8>()
                .copy_from_nonoverlapping(buf.as_ptr(), amt);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/read_buf.rs: 
unsafe { self.unfilled_mut() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/read_buf.rs: 
unsafe { bytes::buf::UninitSlice::from_raw_parts_mut(ptr, len) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/split.rs: 
unsafe { Pin::new_unchecked(&mut *guard) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/stderr.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/stderr.rs: 
unsafe { BorrowedHandle::borrow_raw(self.as_raw_handle()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/stdin.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/stdin.rs: 
unsafe { BorrowedHandle::borrow_raw(self.as_raw_handle()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/stdout.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/stdout.rs: 
unsafe { BorrowedHandle::borrow_raw(self.as_raw_handle()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/util/fill_buf.rs: 
unsafe {
                // Safety: This is necessary only due to a limitation in the
                // borrow checker. Once Rust starts using the polonius borrow
                // checker, this can be simplified.
                //
                // The safety of this transmute relies on the fact that the
                // value of `reader` is `None` when we return in this branch.
                // Otherwise the caller could poll us again after
                // completion, and access the mutable reference while the
                // returned immutable reference still exists.
                let slice = std::mem::transmute::<&[u8], &'a [u8]>(slice);
                Poll::Ready(Ok(slice))
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/util/read_buf.rs: 
unsafe { &mut *(dst as *mut _ as *mut [MaybeUninit<u8>]) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/util/read_buf.rs: 
unsafe {
            me.buf.advance_mut(n);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/util/take.rs: 
unsafe {
            buf.assume_init(n);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/util/vec_with_initialized.rs: 
unsafe { std::slice::from_raw_parts_mut::<'a, MaybeUninit<u8>>(ptr, cap) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/util/vec_with_initialized.rs: 
unsafe {
            read_buf.assume_init(num_initialized);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/io/util/vec_with_initialized.rs: 
unsafe {
            self.num_initialized = parts.initialized;
            vec.set_len(parts.len);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/loom/std/atomic_u16.rs: 
unsafe { &*self.inner.get() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/loom/std/atomic_u32.rs: 
unsafe { &*self.inner.get() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/loom/std/atomic_usize.rs: 
unsafe { (*self.inner.get()).get_mut() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/loom/std/atomic_usize.rs: 
unsafe { &*self.inner.get() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/loom/std/atomic_usize.rs: 
unsafe { &mut *self.inner.get() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/macros/join.rs: 
unsafe { Pin::new_unchecked(fut) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/macros/join.rs: 
unsafe { Pin::new_unchecked(fut) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/macros/pin.rs: 
unsafe {
            $crate::macros::support::Pin::new_unchecked(&mut $x)
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/macros/select.rs: 
unsafe { Pin::new_unchecked(fut) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/macros/try_join.rs: 
unsafe { Pin::new_unchecked(fut) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/macros/try_join.rs: 
unsafe { Pin::new_unchecked(fut) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe { std::net::UdpSocket::from_raw_fd(raw_fd) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe { std::net::UdpSocket::from_raw_socket(raw_socket) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe {
                &mut *(buf.unfilled_mut() as *mut [std::mem::MaybeUninit<u8>] as *mut [u8])
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe {
            buf.assume_init(n);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe { &mut *(dst as *mut _ as *mut [std::mem::MaybeUninit<u8>] as *mut [u8]) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe {
                    buf.advance_mut(n);
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe { &mut *(dst as *mut _ as *mut [std::mem::MaybeUninit<u8>] as *mut [u8]) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe {
                    buf.advance_mut(n);
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe { &mut *(dst as *mut _ as *mut [std::mem::MaybeUninit<u8>] as *mut [u8]) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe {
                    buf.advance_mut(n);
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe { &mut *(dst as *mut _ as *mut [std::mem::MaybeUninit<u8>] as *mut [u8]) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe {
                    buf.advance_mut(n);
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe {
                &mut *(buf.unfilled_mut() as *mut [std::mem::MaybeUninit<u8>] as *mut [u8])
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe {
            buf.assume_init(n);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe {
                &mut *(buf.unfilled_mut() as *mut [std::mem::MaybeUninit<u8>] as *mut [u8])
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe {
            buf.assume_init(n);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/udp.rs: 
unsafe { BorrowedSocket::borrow_raw(self.as_raw_socket()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/listener.rs: 
unsafe { std::net::TcpListener::from_raw_fd(raw_fd) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/listener.rs: 
unsafe { std::net::TcpListener::from_raw_socket(raw_socket) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/listener.rs: 
unsafe { std::net::TcpListener::from_raw_fd(raw_fd) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/listener.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/listener.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/listener.rs: 
unsafe { BorrowedSocket::borrow_raw(self.as_raw_socket()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/socket.rs: 
unsafe { mio::net::TcpStream::from_raw_fd(raw_fd) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/socket.rs: 
unsafe { mio::net::TcpStream::from_raw_socket(raw_socket) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/socket.rs: 
unsafe { mio::net::TcpListener::from_raw_fd(raw_fd) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/socket.rs: 
unsafe { mio::net::TcpListener::from_raw_socket(raw_socket) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/socket.rs: 
unsafe { TcpSocket::from_raw_fd(raw_fd) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/socket.rs: 
unsafe { TcpSocket::from_raw_socket(raw_socket) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/socket.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/socket.rs: 
unsafe { BorrowedSocket::borrow_raw(self.as_raw_socket()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/stream.rs: 
unsafe { std::net::TcpStream::from_raw_fd(raw_fd) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/stream.rs: 
unsafe { std::net::TcpStream::from_raw_socket(raw_socket) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/stream.rs: 
unsafe { std::net::TcpStream::from_raw_fd(raw_fd) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/stream.rs: 
unsafe {
                &mut *(buf.unfilled_mut() as *mut [std::mem::MaybeUninit<u8>] as *mut [u8])
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/stream.rs: 
unsafe { buf.assume_init(ret) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/stream.rs: 
unsafe { &mut *(dst as *mut _ as *mut [std::mem::MaybeUninit<u8>] as *mut [u8]) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/stream.rs: 
unsafe {
                    buf.advance_mut(n);
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/stream.rs: 
unsafe { self.io.poll_read(cx, buf) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/stream.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/stream.rs: 
unsafe { BorrowedSocket::borrow_raw(self.as_raw_socket()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/tcp/stream.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/listener.rs: 
unsafe { net::UnixListener::from_raw_fd(raw_fd) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/listener.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/pipe.rs: 
unsafe { mio_pipe::Sender::from_raw_fd(owned_fd.into_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/pipe.rs: 
unsafe { OwnedFd::from_raw_fd(mio_pipe.into_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/pipe.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/pipe.rs: 
unsafe { mio_pipe::Receiver::from_raw_fd(owned_fd.into_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/pipe.rs: 
unsafe { &mut *(dst as *mut _ as *mut [std::mem::MaybeUninit<u8>] as *mut [u8]) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/pipe.rs: 
unsafe {
                    buf.advance_mut(n);
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/pipe.rs: 
unsafe { OwnedFd::from_raw_fd(mio_pipe.into_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/pipe.rs: 
unsafe { self.io.poll_read(cx, buf) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/pipe.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/pipe.rs: 
unsafe { std::mem::zeroed() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/pipe.rs: 
unsafe { libc::fstat(fd.as_raw_fd(), &mut stat) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/pipe.rs: 
unsafe { libc::fcntl(fd.as_raw_fd(), libc::F_GETFL) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/pipe.rs: 
unsafe { libc::fcntl(fd.as_raw_fd(), libc::F_SETFL, flags) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/pipe.rs: 
unsafe { libc::fcntl(fd.as_raw_fd(), libc::F_GETFL) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/pipe.rs: 
unsafe { libc::fcntl(fd.as_raw_fd(), libc::F_SETFL, new) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/socket.rs: 
unsafe { mio::net::UnixListener::from_raw_fd(raw_fd) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/socket.rs: 
unsafe { mio::net::UnixStream::from_raw_fd(raw_fd) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/socket.rs: 
unsafe { mio::net::UnixDatagram::from_raw_fd(raw_fd) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/socket.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/stream.rs: 
unsafe { &mut *(dst as *mut _ as *mut [std::mem::MaybeUninit<u8>] as *mut [u8]) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/stream.rs: 
unsafe {
                    buf.advance_mut(n);
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/stream.rs: 
unsafe { std::os::unix::net::UnixStream::from_raw_fd(raw_fd) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/stream.rs: 
unsafe { self.io.poll_read(cx, buf) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/stream.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/ucred.rs: 
unsafe {
            let raw_fd = sock.as_raw_fd();

            let mut ucred = ucred {
                pid: 0,
                uid: 0,
                gid: 0,
            };

            let ucred_size = mem::size_of::<ucred>();

            // These paranoid checks should be optimized-out
            assert!(mem::size_of::<u32>() <= mem::size_of::<usize>());
            assert!(ucred_size <= u32::MAX as usize);

            let mut ucred_size = ucred_size as socklen_t;

            let ret = getsockopt(
                raw_fd,
                SOL_SOCKET,
                SO_PEERCRED,
                &mut ucred as *mut ucred as *mut c_void,
                &mut ucred_size,
            );
            if ret == 0 && ucred_size as usize == mem::size_of::<ucred>() {
                Ok(super::UCred {
                    uid: ucred.uid as unix::uid_t,
                    gid: ucred.gid as unix::gid_t,
                    pid: Some(ucred.pid as unix::pid_t),
                })
            } else {
                Err(io::Error::last_os_error())
            }
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/ucred.rs: 
unsafe {
            let raw_fd = sock.as_raw_fd();

            let mut unpcbid = unpcbid {
                unp_pid: 0,
                unp_euid: 0,
                unp_egid: 0,
            };

            let unpcbid_size = size_of::<unpcbid>();
            let mut unpcbid_size = unpcbid_size as socklen_t;

            let ret = getsockopt(
                raw_fd,
                SOL_SOCKET,
                LOCAL_PEEREID,
                &mut unpcbid as *mut unpcbid as *mut c_void,
                &mut unpcbid_size,
            );
            if ret == 0 && unpcbid_size as usize == size_of::<unpcbid>() {
                Ok(super::UCred {
                    uid: unpcbid.unp_euid as unix::uid_t,
                    gid: unpcbid.unp_egid as unix::gid_t,
                    pid: Some(unpcbid.unp_pid as unix::pid_t),
                })
            } else {
                Err(io::Error::last_os_error())
            }
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/ucred.rs: 
unsafe {
            let raw_fd = sock.as_raw_fd();

            let mut uid = MaybeUninit::uninit();
            let mut gid = MaybeUninit::uninit();

            let ret = getpeereid(raw_fd, uid.as_mut_ptr(), gid.as_mut_ptr());

            if ret == 0 {
                Ok(super::UCred {
                    uid: uid.assume_init() as unix::uid_t,
                    gid: gid.assume_init() as unix::gid_t,
                    pid: None,
                })
            } else {
                Err(io::Error::last_os_error())
            }
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/ucred.rs: 
unsafe {
            let raw_fd = sock.as_raw_fd();

            let mut uid = MaybeUninit::uninit();
            let mut gid = MaybeUninit::uninit();
            let mut pid: MaybeUninit<pid_t> = MaybeUninit::uninit();
            let mut pid_size: MaybeUninit<u32> = MaybeUninit::new(size_of::<pid_t>() as u32);

            if getsockopt(
                raw_fd,
                SOL_LOCAL,
                LOCAL_PEEREPID,
                pid.as_mut_ptr() as *mut c_void,
                pid_size.as_mut_ptr(),
            ) != 0
            {
                return Err(io::Error::last_os_error());
            }

            assert!(pid_size.assume_init() == (size_of::<pid_t>() as u32));

            let ret = getpeereid(raw_fd, uid.as_mut_ptr(), gid.as_mut_ptr());

            if ret == 0 {
                Ok(super::UCred {
                    uid: uid.assume_init() as unix::uid_t,
                    gid: gid.assume_init() as unix::gid_t,
                    pid: Some(pid.assume_init() as unix::pid_t),
                })
            } else {
                Err(io::Error::last_os_error())
            }
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/ucred.rs: 
unsafe {
            let raw_fd = sock.as_raw_fd();

            let mut cred = ptr::null_mut();
            let ret = libc::getpeerucred(raw_fd, &mut cred);

            if ret == 0 {
                let uid = libc::ucred_geteuid(cred);
                let gid = libc::ucred_getegid(cred);
                let pid = libc::ucred_getpid(cred);

                libc::ucred_free(cred);

                Ok(super::UCred {
                    uid: uid as unix::uid_t,
                    gid: gid as unix::gid_t,
                    pid: Some(pid as unix::pid_t),
                })
            } else {
                Err(io::Error::last_os_error())
            }
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/ucred.rs: 
unsafe {
            let raw_fd = sock.as_raw_fd();

            let mut uid = std::mem::MaybeUninit::uninit();
            let mut gid = std::mem::MaybeUninit::uninit();

            let ret = libc::getpeereid(raw_fd, uid.as_mut_ptr(), gid.as_mut_ptr());

            if ret == 0 {
                Ok(super::UCred {
                    uid: uid.assume_init(),
                    gid: gid.assume_init(),
                    pid: None,
                })
            } else {
                Err(io::Error::last_os_error())
            }
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/datagram/socket.rs: 
unsafe { std::os::unix::net::UnixDatagram::from_raw_fd(raw_fd) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/datagram/socket.rs: 
unsafe { &mut *(dst as *mut _ as *mut [std::mem::MaybeUninit<u8>] as *mut [u8]) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/datagram/socket.rs: 
unsafe {
                    buf.advance_mut(n);
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/datagram/socket.rs: 
unsafe { &mut *(dst as *mut _ as *mut [std::mem::MaybeUninit<u8>] as *mut [u8]) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/datagram/socket.rs: 
unsafe {
                    buf.advance_mut(n);
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/datagram/socket.rs: 
unsafe { &mut *(dst as *mut _ as *mut [std::mem::MaybeUninit<u8>] as *mut [u8]) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/datagram/socket.rs: 
unsafe {
                    buf.advance_mut(n);
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/datagram/socket.rs: 
unsafe { &mut *(dst as *mut _ as *mut [std::mem::MaybeUninit<u8>] as *mut [u8]) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/datagram/socket.rs: 
unsafe {
                    buf.advance_mut(n);
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/datagram/socket.rs: 
unsafe {
                &mut *(buf.unfilled_mut() as *mut [std::mem::MaybeUninit<u8>] as *mut [u8])
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/datagram/socket.rs: 
unsafe {
            buf.assume_init(n);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/datagram/socket.rs: 
unsafe {
                &mut *(buf.unfilled_mut() as *mut [std::mem::MaybeUninit<u8>] as *mut [u8])
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/datagram/socket.rs: 
unsafe {
            buf.assume_init(n);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/unix/datagram/socket.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/windows/named_pipe.rs: 
unsafe { named_pipe_info(self.io.as_raw_handle()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/windows/named_pipe.rs: 
unsafe { &mut *(dst as *mut _ as *mut [std::mem::MaybeUninit<u8>] as *mut [u8]) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/windows/named_pipe.rs: 
unsafe {
                    buf.advance_mut(n);
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/windows/named_pipe.rs: 
unsafe { self.io.poll_read(cx, buf) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/windows/named_pipe.rs: 
unsafe { BorrowedHandle::borrow_raw(self.as_raw_handle()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/windows/named_pipe.rs: 
unsafe { named_pipe_info(self.io.as_raw_handle()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/windows/named_pipe.rs: 
unsafe { &mut *(dst as *mut _ as *mut [std::mem::MaybeUninit<u8>] as *mut [u8]) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/windows/named_pipe.rs: 
unsafe {
                    buf.advance_mut(n);
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/windows/named_pipe.rs: 
unsafe { self.io.poll_read(cx, buf) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/windows/named_pipe.rs: 
unsafe { BorrowedHandle::borrow_raw(self.as_raw_handle()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/windows/named_pipe.rs: 
unsafe {
    ///     assert_eq!(
    ///         ERROR_SUCCESS,
    ///         SetSecurityInfo(
    ///             pipe.as_raw_handle() as _,
    ///             SE_KERNEL_OBJECT,
    ///             DACL_SECURITY_INFORMATION,
    ///             ptr::null_mut(),
    ///             ptr::null_mut(),
    ///             ptr::null_mut(),
    ///             ptr::null_mut(),
    ///         )
    ///     );
    /// } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/windows/named_pipe.rs: 
unsafe {
    ///     assert_eq!(
    ///         ERROR_ACCESS_DENIED,
    ///         SetSecurityInfo(
    ///             pipe.as_raw_handle() as _,
    ///             SE_KERNEL_OBJECT,
    ///             DACL_SECURITY_INFORMATION,
    ///             ptr::null_mut(),
    ///             ptr::null_mut(),
    ///             ptr::null_mut(),
    ///             ptr::null_mut(),
    ///         )
    ///     );
    /// } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/windows/named_pipe.rs: 
unsafe { self.create_with_security_attributes_raw(addr, ptr::null_mut()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/net/windows/named_pipe.rs: 
unsafe { self.open_with_security_attributes_raw(addr, ptr::null_mut()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/process/mod.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/process/mod.rs: 
unsafe { BorrowedHandle::borrow_raw(self.as_raw_handle()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/process/windows.rs: 
unsafe {
                RegisterWaitForSingleObject(
                    &mut wait_object,
                    inner.child.as_raw_handle() as _,
                    Some(callback),
                    ptr as *mut _,
                    INFINITE,
                    WT_EXECUTEINWAITTHREAD | WT_EXECUTEONLYONCE,
                )
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/process/windows.rs: 
unsafe { Box::from_raw(ptr) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/process/windows.rs: 
unsafe {
            let rc = UnregisterWaitEx(self.wait_object, INVALID_HANDLE_VALUE);
            if rc == 0 {
                panic!("failed to unregister: {}", io::Error::last_os_error());
            }
            drop(Box::from_raw(self.tx));
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/process/windows.rs: 
unsafe { StdFile::from_raw_handle(io.into_raw_handle()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/process/windows.rs: 
unsafe {
        let mut dup_handle = INVALID_HANDLE_VALUE;
        let cur_proc = GetCurrentProcess();

        let status = DuplicateHandle(
            cur_proc,
            io.as_raw_handle() as _,
            cur_proc,
            &mut dup_handle,
            0,
            0,
            DUPLICATE_SAME_ACCESS,
        );

        if status == 0 {
            return Err(io::Error::last_os_error());
        }

        Ok(StdFile::from_raw_handle(dup_handle as _))
    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/process/unix/mod.rs: 
unsafe { File::from_raw_fd(fd.into_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/process/unix/mod.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/process/unix/mod.rs: 
unsafe { BorrowedFd::borrow_raw(self.as_raw_fd()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/process/unix/mod.rs: 
unsafe { self.inner.poll_read(cx, buf) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/process/unix/mod.rs: 
unsafe {
        let fd = fd.as_raw_fd();
        let previous = libc::fcntl(fd, libc::F_GETFL);
        if previous == -1 {
            return Err(io::Error::last_os_error());
        }

        let new = if nonblocking {
            previous | libc::O_NONBLOCK
        } else {
            previous & !libc::O_NONBLOCK
        };

        let r = libc::fcntl(fd, libc::F_SETFL, new);
        if r == -1 {
            return Err(io::Error::last_os_error());
        }
    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/process/unix/pidfd_reaper.rs: 
unsafe { syscall(SYS_pidfd_open, pid, PIDFD_NONBLOCK) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/process/unix/pidfd_reaper.rs: 
unsafe { File::from_raw_fd(fd as i32) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/park.rs: 
unsafe {
            let raw = unparker_to_raw_waker(self.inner);
            Waker::from_raw(raw)
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/context/scoped.rs: 
unsafe { f(Some(&*val)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/io/driver.rs: 
unsafe { &*ptr } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/io/driver.rs: 
unsafe {
                self.registrations
                    .remove(&mut self.synced.lock(), &scheduled_io)
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/io/registration_set.rs: 
unsafe { self.remove(synced, &io) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/io/registration_set.rs: 
unsafe { NonNull::new_unchecked(Arc::as_ptr(io).cast_mut()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/io/registration_set.rs: 
unsafe { NonNull::new_unchecked(Arc::as_ptr(handle) as *mut _) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/io/registration_set.rs: 
unsafe { Arc::from_raw(ptr.as_ptr() as *const _) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/io/scheduled_io.rs: 
unsafe { &mut *waiter.as_ptr() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/io/scheduled_io.rs: 
unsafe {
            let me = self.get_unchecked_mut();
            (&me.scheduled_io, &mut me.state, &me.waiter)
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/io/scheduled_io.rs: 
unsafe { (*waiter.get()).interest } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/io/scheduled_io.rs: 
unsafe {
                        (*waiter.get()).waker = Some(cx.waker().clone());
                    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/io/scheduled_io.rs: 
unsafe { NonNull::new_unchecked(waiter.get()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/io/scheduled_io.rs: 
unsafe { &mut *waiter.get() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/io/scheduled_io.rs: 
unsafe { &mut *waiter.get() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/io/scheduled_io.rs: 
unsafe {
            waiters
                .list
                .remove(NonNull::new_unchecked(self.waiter.get()))
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/local_runtime/runtime.rs: 
unsafe {
            if std::mem::size_of::<F>() > BOX_FUTURE_THRESHOLD {
                self.handle.spawn_local_named(Box::pin(future), meta)
            } else {
                self.handle.spawn_local_named(future, meta)
            }
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/inject.rs: 
unsafe { self.shared.push(&mut synced, task) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/inject.rs: 
unsafe { self.shared.pop(&mut synced) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/inject/rt_multi_thread.rs: 
unsafe { prev.set_queue_next(Some(next)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/inject/rt_multi_thread.rs: 
unsafe { batch_tail.get_queue_next().is_none() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/inject/rt_multi_thread.rs: 
unsafe { task::Notified::<T>::from_raw(task) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/inject/rt_multi_thread.rs: 
unsafe {
                tail.set_queue_next(Some(batch_head));
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/inject/shared.rs: 
unsafe { task.get_queue_next().is_none() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/inject/shared.rs: 
unsafe { tail.set_queue_next(Some(task)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/inject/synced.rs: 
unsafe { task.get_queue_next() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/inject/synced.rs: 
unsafe { task.set_queue_next(None) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/inject/synced.rs: 
unsafe { task::Notified::from_raw(task) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread/queue.rs: 
unsafe { Box::from_raw(Box::into_raw(buffer).cast()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread/queue.rs: 
unsafe { self.inner.tail.unsync_load() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread/queue.rs: 
unsafe {
                    ptr::write((*ptr).as_mut_ptr(), task);
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread/queue.rs: 
unsafe { self.inner.tail.unsync_load() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread/queue.rs: 
unsafe {
                ptr::write((*ptr).as_mut_ptr(), task);
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread/queue.rs: 
unsafe { ptr::read((*ptr).as_ptr()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread/queue.rs: 
unsafe { self.inner.tail.unsync_load() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread/queue.rs: 
unsafe { ptr::read(ptr).assume_init() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread/queue.rs: 
unsafe { dst.inner.tail.unsync_load() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread/queue.rs: 
unsafe { ptr::read((*ptr).as_ptr()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread/queue.rs: 
unsafe { ptr::read((*ptr).as_ptr()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread/queue.rs: 
unsafe { ptr::write((*ptr).as_mut_ptr(), task) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread/worker.rs: 
unsafe { worker.inject().pop_n(&mut synced.inject, n) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread/worker.rs: 
unsafe { self.shared.inject.pop(&mut synced.inject) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread/worker.rs: 
unsafe {
            self.shared.inject.push(&mut synced.inject, task);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread/worker.rs: 
unsafe {
            self.shared.inject.push_batch(self, iter);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread/worker/taskdump.rs: 
unsafe { trace_multi_thread(owned, &mut local, synced, injection) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread_alt/queue.rs: 
unsafe { self.inner.tail.unsync_load() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread_alt/queue.rs: 
unsafe {
                    ptr::write((*ptr).as_mut_ptr(), task);
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread_alt/queue.rs: 
unsafe { self.inner.tail.unsync_load() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread_alt/queue.rs: 
unsafe {
                ptr::write((*ptr).as_mut_ptr(), task);
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread_alt/queue.rs: 
unsafe { ptr::read((*ptr).as_ptr()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread_alt/queue.rs: 
unsafe { self.inner.tail.unsync_load() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread_alt/queue.rs: 
unsafe { ptr::read(ptr).assume_init() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread_alt/queue.rs: 
unsafe { dst.inner.tail.unsync_load() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread_alt/queue.rs: 
unsafe { ptr::read((*ptr).as_ptr()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread_alt/queue.rs: 
unsafe { ptr::read((*ptr).as_ptr()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread_alt/queue.rs: 
unsafe { ptr::write((*ptr).as_mut_ptr(), task) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread_alt/worker.rs: 
unsafe { cx.shared().inject.pop_n(&mut synced.inject, n) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread_alt/worker.rs: 
unsafe { self.inject.pop(&mut synced.inject) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread_alt/worker.rs: 
unsafe {
            self.inject.push(&mut synced.inject, task);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread_alt/worker.rs: 
unsafe {
            self.inject.push_batch(self, iter);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread_alt/worker.rs: 
unsafe {
            self.inject.push_batch(&mut synced.inject, iter);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/scheduler/multi_thread_alt/worker/taskdump.rs: 
unsafe { trace_multi_thread(owned, &mut local, synced, injection) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/signal/mod.rs: 
unsafe { std::os::unix::net::UnixStream::from_raw_fd(receiver_fd) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/abort.rs: 
unsafe { Header::get_id(self.raw.header_ptr()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/abort.rs: 
unsafe { Header::get_id_ptr(self.raw.header_ptr()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/abort.rs: 
unsafe { id_ptr.as_ref() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/core.rs: 
unsafe { Header::get_trailer(NonNull::from(header)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/core.rs: 
unsafe { Header::get_scheduler::<S>(NonNull::from(header)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/core.rs: 
unsafe { Header::get_id_ptr(NonNull::from(header)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/core.rs: 
unsafe {
                check(
                    &result.header,
                    &result.trailer,
                    &result.core.scheduler,
                    &result.core.task_id,
                );
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/core.rs: 
unsafe { &mut *ptr } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/core.rs: 
unsafe { Pin::new_unchecked(future) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/core.rs: 
unsafe {
            self.set_stage(Stage::Consumed);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/core.rs: 
unsafe {
            self.set_stage(Stage::Finished(output));
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/core.rs: 
unsafe { &mut *ptr } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/core.rs: 
unsafe { self.owner_id.with(|ptr| *ptr) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/core.rs: 
unsafe { &*ptr } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/harness.rs: 
unsafe { &*self.header_ptr().as_ptr() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/harness.rs: 
unsafe { &self.cell.as_ref().trailer } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/harness.rs: 
unsafe { &self.cell.as_ref().core } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/harness.rs: 
unsafe {
            drop(Box::from_raw(self.cell.as_ptr()));
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/harness.rs: 
unsafe { Task::from_raw(self.cell.cast()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/harness.rs: 
unsafe { trailer.will_wake(waker) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/harness.rs: 
unsafe {
        trailer.set_waker(Some(waker));
    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/harness.rs: 
unsafe {
            trailer.set_waker(None);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/join.rs: 
unsafe { Header::get_id(self.raw.header_ptr()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/join.rs: 
unsafe {
            self.raw
                .try_read_output(&mut ret as *mut _ as *mut (), cx.waker());
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/join.rs: 
unsafe { Header::get_id_ptr(self.raw.header_ptr()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/join.rs: 
unsafe { id_ptr.as_ref() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/list.rs: 
unsafe { self.bind_inner(task, notified) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/list.rs: 
unsafe { self.bind_inner(task, notified) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/list.rs: 
unsafe {
            // safety: We just created the task, so we have exclusive access
            // to the field.
            task.header().set_owner_id(self.id);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/list.rs: 
unsafe { self.list.remove(task.header_ptr()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/list.rs: 
unsafe {
            // safety: We just created the task, so we have exclusive access
            // to the field.
            task.header().set_owner_id(self.id);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/list.rs: 
unsafe { inner.list.remove(task.header_ptr()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/list.rs: 
unsafe { f(&mut *ptr) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/mod.rs: 
unsafe { Notified(Task::new(self.raw)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/mod.rs: 
unsafe { Header::get_id(self.raw.header_ptr()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/mod.rs: 
unsafe { Header::get_id(target) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/raw.rs: 
unsafe { NonNull::new_unchecked(ptr.cast()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/raw.rs: 
unsafe { Header::get_trailer(self.ptr) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/raw.rs: 
unsafe { self.ptr.as_ref() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/raw.rs: 
unsafe { &*self.trailer_ptr().as_ptr() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/raw.rs: 
unsafe { (vtable.poll)(self.ptr) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/raw.rs: 
unsafe { (vtable.schedule)(self.ptr) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/raw.rs: 
unsafe {
            (vtable.dealloc)(self.ptr);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/raw.rs: 
unsafe { (vtable.drop_join_handle_slow)(self.ptr) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/raw.rs: 
unsafe { (vtable.drop_abort_handle)(self.ptr) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/raw.rs: 
unsafe { (vtable.shutdown)(self.ptr) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/waker.rs: 
unsafe { ManuallyDrop::new(Waker::from_raw(raw_waker(*header))) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/trace/mod.rs: 
unsafe {
            Self::try_with_current(|context| f(&context.collector)).expect(FAIL_NO_THREAD_LOCAL)
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/trace/mod.rs: 
unsafe {
                ManuallyDrop::take(&mut self.0)();
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/task/trace/mod.rs: 
unsafe {
            let mut frame = Frame {
                inner_addr: Self::poll as *const c_void,
                parent: None,
            };

            Context::with_current_frame(|current| {
                frame.parent = current.take();
                current.set(Some(NonNull::from(&frame)));
            });

            let _restore = defer(|| {
                Context::with_current_frame(|current| {
                    current.set(frame.parent);
                });
            });

            let this = self.project();
            this.future.poll(cx)
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/tests/inject.rs: 
unsafe { inject.push(&mut synced, task) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/tests/inject.rs: 
unsafe { inject.pop(&mut synced) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/tests/inject.rs: 
unsafe { inject.pop(&mut synced) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/tests/inject.rs: 
unsafe {
        inject.push_batch(
            &mut inject_synced,
            (0..10).map(|_| super::unowned(async {}).0),
        );

        assert_eq!(5, inject.pop_n(&mut inject_synced, 5).count());
        assert_eq!(5, inject.pop_n(&mut inject_synced, 5).count());
        assert_eq!(0, inject.pop_n(&mut inject_synced, 5).count());
    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/tests/inject.rs: 
unsafe {
        inject.push_batch(
            &mut inject_synced,
            (0..10).map(|_| super::unowned(async {}).0),
        );
        let _ = inject.pop_n(&mut inject_synced, 10);

        assert_eq!(inject.len(), 0);
    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/tests/loom_multi_thread.rs: 
unsafe { Some(std::ptr::read(self.inner.as_ptr())) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/tests/loom_multi_thread_alt.rs: 
unsafe { Some(std::ptr::read(self.inner.as_ptr())) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/tests/task_combinations.rs: 
unsafe {
                let me = Pin::into_inner_unchecked(self);
                let inner = Pin::new_unchecked(&mut me.inner);
                inner.poll(cx)
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/entry.rs: 
unsafe { self.result.with(|p| *p) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/entry.rs: 
unsafe { self.result.with_mut(|p| *p = result) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/entry.rs: 
unsafe { &*self.inner.get() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/entry.rs: 
unsafe { &*self.inner.get() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/entry.rs: 
unsafe {
                *self.inner.get() = Some(TimerShared::new(shard_id));
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/entry.rs: 
unsafe { self.driver().clear_entry(NonNull::from(self.inner())) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/entry.rs: 
unsafe { self.as_mut().get_unchecked_mut() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/entry.rs: 
unsafe {
                self.driver()
                    .reregister(&self.driver.driver().io, tick, self.inner().into());
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/entry.rs: 
unsafe { self.inner.as_ref().cached_when() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/entry.rs: 
unsafe { self.inner.as_ref().sync_when() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/entry.rs: 
unsafe { self.inner.as_ref().state.is_pending() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/entry.rs: 
unsafe { Pin::new_unchecked(self) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/mod.rs: 
unsafe {} 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/mod.rs: 
unsafe { entry.is_pending() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/mod.rs: 
unsafe { entry.fire(Ok(())) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/mod.rs: 
unsafe {
            let wheels_lock = self.inner.wheels.read();
            let mut lock = wheels_lock.lock_sharded_wheel(entry.as_ref().shard_id());

            if entry.as_ref().might_be_registered() {
                lock.remove(entry);
            }

            entry.as_ref().handle().fire(Ok(()));
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/mod.rs: 
unsafe { entry.as_ref().might_be_registered() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/mod.rs: 
unsafe { entry.fire(Err(crate::time::error::Error::shutdown())) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/mod.rs: 
unsafe { lock.insert(entry) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/mod.rs: 
unsafe {
                        entry.fire(Ok(()))
                    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/mod.rs: 
unsafe { self.0.get_unchecked(index as usize) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/wheel/level.rs: 
unsafe { item.as_ref().cached_when() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/wheel/level.rs: 
unsafe { self.slot[slot].remove(item) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/wheel/mod.rs: 
unsafe {
            self.levels[level].add_entry(item);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/wheel/mod.rs: 
unsafe {
            let when = item.as_ref().cached_when();
            if when == u64::MAX {
                self.pending.remove(item);
            } else {
                debug_assert!(
                    self.elapsed <= when,
                    "elapsed={}; when={}",
                    self.elapsed,
                    when
                );

                let level = self.level_for(when);
                self.levels[level].remove_entry(item);
            }
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/wheel/mod.rs: 
unsafe { item.cached_when() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/wheel/mod.rs: 
unsafe { item.mark_pending(expiration.deadline) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/runtime/time/wheel/mod.rs: 
unsafe {
                        self.levels[level].add_entry(item);
                    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/signal/reusable_box.rs: 
unsafe { NonNull::new_unchecked(boxed) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/signal/reusable_box.rs: 
unsafe { self.boxed.as_ref() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/signal/reusable_box.rs: 
unsafe {
                self.set_same_layout(future);
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/signal/reusable_box.rs: 
unsafe { Pin::new_unchecked(self.boxed.as_mut()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/signal/reusable_box.rs: 
unsafe {
            drop(Box::from_raw(self.boxed.as_ptr()));
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/signal/unix.rs: 
unsafe {
            signal_hook_registry::register(signal, move || action(globals, signal)).map(|_| ())
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/signal/windows/sys.rs: 
unsafe {
        let rc = console::SetConsoleCtrlHandler(Some(handler), 1);
        let ret = if rc == 0 {
            Err(io::Error::last_os_error())
        } else {
            Ok(())
        };

        init = Some(ret);
    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/signal/windows/sys.rs: 
unsafe {
            super::handler(console::CTRL_C_EVENT);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/signal/windows/sys.rs: 
unsafe {
                super::handler(console::CTRL_BREAK_EVENT);
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/signal/windows/sys.rs: 
unsafe {
                super::handler(console::CTRL_CLOSE_EVENT);
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/signal/windows/sys.rs: 
unsafe {
                super::handler(console::CTRL_SHUTDOWN_EVENT);
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/signal/windows/sys.rs: 
unsafe {
                super::handler(console::CTRL_LOGOFF_EVENT);
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/batch_semaphore.rs: 
unsafe { waiter.as_mut().waker.with_mut(|waker| (*waker).take()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/batch_semaphore.rs: 
unsafe { waiter.as_mut().waker.with_mut(|waker| (*waker).take()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/batch_semaphore.rs: 
unsafe { &mut *waker } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/batch_semaphore.rs: 
unsafe {
                let node = Pin::into_inner_unchecked(node) as *mut _;
                NonNull::new_unchecked(node)
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/batch_semaphore.rs: 
unsafe {
            // Safety: all fields other than `node` are `Unpin`

            is_unpin::<&Semaphore>();
            is_unpin::<&mut bool>();
            is_unpin::<usize>();

            let this = self.get_unchecked_mut();
            (
                Pin::new_unchecked(&mut this.node),
                this.semaphore,
                this.num_permits,
                &mut this.queued,
            )
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/batch_semaphore.rs: 
unsafe { waiters.queue.remove(node) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/broadcast.rs: 
unsafe { Sender::new_with_receiver_count(1, capacity) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/broadcast.rs: 
unsafe { Self::new_with_receiver_count(0, capacity) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/broadcast.rs: 
unsafe {
                            // Safety: accessing `waker` is safe because
                            // the tail lock is held.
                            if let Some(waker) = (*waiter.as_ptr()).waker.take() {
                                wakers.push(waker);
                            }

                            // Safety: `queued` is atomic.
                            let queued = &(*waiter.as_ptr()).queued;
                            // `Relaxed` suffices because the tail lock is held.
                            assert!(queued.load(Relaxed));
                            // `Release` is needed to synchronize with `Recv::drop`.
                            // It is critical to set this variable **after** waker
                            // is extracted, otherwise we may data race with `Recv::drop`.
                            queued.store(false, Release);
                        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/broadcast.rs: 
unsafe {
            // Safety: Receiver is Unpin
            is_unpin::<&mut Receiver<T>>();

            let me = self.get_unchecked_mut();
            (me.receiver, &me.waiter)
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/broadcast.rs: 
unsafe { (*ptr).queued.load(Acquire) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/broadcast.rs: 
unsafe { (*ptr).queued.load(Relaxed) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/broadcast.rs: 
unsafe {
                    self.waiter.with_mut(|ptr| {
                        tail.waiters.remove((&mut *ptr).into());
                    });
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/broadcast.rs: 
unsafe { (*ptr).clone() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/broadcast.rs: 
unsafe { *ptr = None } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mutex.rs: 
unsafe {
            // Safety: This is https://github.com/rust-lang/rust/pull/76936
            &mut *self.c.get()
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mutex.rs: 
unsafe { std::ptr::read(&me.resource_span) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mutex.rs: 
unsafe { &*self.lock.c.get() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mutex.rs: 
unsafe { &mut *self.lock.c.get() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mutex.rs: 
unsafe {
            OwnedMutexGuardInner {
                lock: ptr::read(&me.lock),
                #[cfg(all(tokio_unstable, feature = "tracing"))]
                resource_span: ptr::read(&me.resource_span),
            }
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mutex.rs: 
unsafe { &*self.lock.c.get() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mutex.rs: 
unsafe { &mut *self.lock.c.get() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mutex.rs: 
unsafe { std::ptr::read(&me.resource_span) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mutex.rs: 
unsafe { &*self.data } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mutex.rs: 
unsafe { &mut *self.data } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mutex.rs: 
unsafe {
            OwnedMappedMutexGuardInner {
                data: me.data,
                lock: ptr::read(&me.lock),
                #[cfg(all(tokio_unstable, feature = "tracing"))]
                resource_span: ptr::read(&me.resource_span),
            }
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mutex.rs: 
unsafe { &*self.data } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mutex.rs: 
unsafe { &mut *self.data } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/notify.rs: 
unsafe { waiter.as_ref() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/notify.rs: 
unsafe { waiter.as_ref() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/notify.rs: 
unsafe { waiter.waker.with_mut(|waker| (*waker).take()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/notify.rs: 
unsafe { waiter.as_ref() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/notify.rs: 
unsafe { waiter.waker.with_mut(|waker| (*waker).take()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/notify.rs: 
unsafe {
            // Safety: `notify`, `state` and `notify_waiters_calls` are `Unpin`.

            is_unpin::<&Notify>();
            is_unpin::<State>();
            is_unpin::<usize>();

            let me = self.get_unchecked_mut();
            (
                me.notify,
                &mut me.state,
                &me.notify_waiters_calls,
                &me.waiter,
            )
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/notify.rs: 
unsafe {
                            old_waker =
                                waiter.waker.with_mut(|v| std::mem::replace(&mut *v, waker));
                        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/notify.rs: 
unsafe { waiter.waker.with_mut(|waker| (*waker).take()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/notify.rs: 
unsafe { waiter.waker.with_mut(|waker| (*waker).take()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/notify.rs: 
unsafe { waiter.waker.with_mut(|waker| (*waker).take()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/notify.rs: 
unsafe { waiters.remove(NonNull::from(waiter)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/notify.rs: 
unsafe { Pin::new_unchecked(self).project() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/notify.rs: 
unsafe { waiters.remove(NonNull::from(waiter)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/once_cell.rs: 
unsafe {
                self.value
                    .with_mut(|ptr| ptr::drop_in_place((*ptr).as_mut_ptr()));
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/once_cell.rs: 
unsafe {
            self.value.with_mut(|ptr| (*ptr).as_mut_ptr().write(value));
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/once_cell.rs: 
unsafe { self.get_unchecked() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/once_cell.rs: 
unsafe { self.get_unchecked() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/once_cell.rs: 
unsafe { self.get_unchecked_mut() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/once_cell.rs: 
unsafe { self.get_unchecked() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/once_cell.rs: 
unsafe { self.get_unchecked() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/once_cell.rs: 
unsafe { Ok(self.get_unchecked()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/once_cell.rs: 
unsafe { Ok(self.get_unchecked()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/once_cell.rs: 
unsafe { self.value.with(|ptr| ptr::read(ptr).assume_init()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe {
            // SAFETY: The receiver will not access the `UnsafeCell` unless the
            // channel has been marked as "complete" (the `VALUE_SENT` state bit
            // is set).
            // That bit is only set by the sender later on in this method, and
            // calling this method consumes `self`. Therefore, if it was possible to
            // call this method, we know that the `VALUE_SENT` bit is unset, and
            // the receiver is not currently accessing the `UnsafeCell`.
            *ptr = Some(t);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe {
                // SAFETY: The receiver will not access the `UnsafeCell` unless
                // the channel has been marked as "complete". Calling
                // `complete()` will return true if this bit is set, and false
                // if it is not set. Thus, if `complete()` returned false, it is
                // safe for us to access the value, because we know that the
                // receiver will not.
                return Err(inner.consume_value().unwrap());
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe { inner.tx_task.will_wake(cx) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe { inner.tx_task.drop_task() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe {
                inner.tx_task.set_task(cx);
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe { inner.consume_value() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe { inner.consume_value() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe {
                self.rx_task.with_task(Waker::wake_by_ref);
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe { self.consume_value() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe { self.rx_task.will_wake(cx) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe { self.consume_value() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe { self.rx_task.drop_task() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe {
                    self.rx_task.set_task(cx);
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe { self.consume_value() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe {
                self.tx_task.with_task(Waker::wake_by_ref);
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe {
                self.rx_task.drop_task();
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe {
                self.tx_task.drop_task();
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/oneshot.rs: 
unsafe {
            // Note: the assertion holds because if the value has been sent by sender,
            // we must ensure that the value must have been consumed by the receiver before
            // dropping the `Inner`.
            debug_assert!(self.consume_value().is_none());
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/rwlock.rs: 
unsafe {
            // Safety: This is https://github.com/rust-lang/rust/pull/76936
            &mut *self.c.get()
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/block.rs: 
unsafe {
            // Allocate the block on the heap.
            // SAFETY: The size of the Block<T> is non-zero, since it is at least the size of the header.
            let block = std::alloc::alloc(Layout::new::<Block<T>>()) as *mut Block<T>;
            let block = match NonNull::new(block) {
                Some(block) => block,
                None => std::alloc::handle_alloc_error(Layout::new::<Block<T>>()),
            };

            // Write the header to the block.
            Block::addr_of_header(block).as_ptr().write(BlockHeader {
                // The absolute index in the channel of the first slot in the block.
                start_index,

                // Pointer to the next block in the linked list.
                next: AtomicPtr::new(ptr::null_mut()),

                ready_slots: AtomicUsize::new(0),

                observed_tail_position: UnsafeCell::new(0),
            });

            // Initialize the values array.
            Values::initialize(Block::addr_of_values(block));

            // Convert the pointer to a `Box`.
            // Safety: The raw pointer was allocated using the global allocator, and with
            // the layout for a `Block<T>`, so it's valid to convert it to box.
            Box::from_raw(block.as_ptr())
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/block.rs: 
unsafe { *ptr } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/block.rs: 
unsafe {
            ret.map_or(true, |block| {
                block.as_ref().header.start_index == self.header.start_index.wrapping_add(BLOCK_CAP)
            })
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/block.rs: 
unsafe { NonNull::new_unchecked(Box::into_raw(new_block)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/block.rs: 
unsafe { curr.as_ref().try_push(&mut new_block, AcqRel, Acquire) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/chan.rs: 
unsafe { &mut *rx_fields_ptr } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/chan.rs: 
unsafe { &*rx_fields_ptr } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/chan.rs: 
unsafe { &*rx_fields_ptr } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/chan.rs: 
unsafe { &mut *rx_fields_ptr } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/chan.rs: 
unsafe { &mut *rx_fields_ptr } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/chan.rs: 
unsafe { &mut *rx_fields_ptr } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/chan.rs: 
unsafe { &mut *rx_fields_ptr } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/chan.rs: 
unsafe { &mut *rx_fields_ptr } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/chan.rs: 
unsafe { rx_fields.list.free_blocks() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/list.rs: 
unsafe {
            // Write the value to the block
            block.as_ref().write(slot_index, value);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/list.rs: 
unsafe { block.as_ref().tx_close() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/list.rs: 
unsafe { &*block_ptr } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/list.rs: 
unsafe { &(*block_ptr) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/list.rs: 
unsafe { NonNull::new_unchecked(block_ptr) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/list.rs: 
unsafe {
                        block.tx_release(tail_position);
                    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/list.rs: 
unsafe {
            let tail_block = &*tail;
            tail_block.is_closed()
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/list.rs: 
unsafe { self.head.as_ref() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/list.rs: 
unsafe {
            let block = self.head.as_ref();

            let ret = block.read(self.index);

            if let Some(block::Read::Value(..)) = ret {
                self.index = self.index.wrapping_add(1);
            }

            ret
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/list.rs: 
unsafe { self.head.as_ref() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/mpsc/list.rs: 
unsafe {
                // Get a handle to the block that will be freed and update
                // `free_head` to point to the next block.
                let block = self.free_head;

                let observed_tail_position = block.as_ref().observed_tail_position();

                let required_index = match observed_tail_position {
                    Some(i) => i,
                    None => return,
                };

                if required_index > self.index {
                    return;
                }

                // We may read the next pointer with `Relaxed` ordering as it is
                // guaranteed that the `reclaim_blocks` routine trails the `recv`
                // routine. Any memory accessed by `reclaim_blocks` has already
                // been acquired by `recv`.
                let next_block = block.as_ref().load_next(Relaxed);

                // Update the free list head
                self.free_head = next_block.unwrap();

                // Push the emptied block onto the back of the queue, making it
                // available to senders.
                tx.reclaim_block(block);
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/rwlock/owned_read_guard.rs: 
unsafe {
            Inner {
                #[cfg(all(tokio_unstable, feature = "tracing"))]
                resource_span: ptr::read(&me.resource_span),
                lock: ptr::read(&me.lock),
                data: me.data,
            }
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/rwlock/owned_read_guard.rs: 
unsafe { &*self.data } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/rwlock/owned_write_guard.rs: 
unsafe {
            Inner {
                #[cfg(all(tokio_unstable, feature = "tracing"))]
                resource_span: ptr::read(&me.resource_span),
                permits_acquired: me.permits_acquired,
                lock: ptr::read(&me.lock),
                data: me.data,
            }
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/rwlock/owned_write_guard.rs: 
unsafe { &*self.data } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/rwlock/owned_write_guard.rs: 
unsafe { &mut *self.data } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/rwlock/owned_write_guard_mapped.rs: 
unsafe {
            Inner {
                #[cfg(all(tokio_unstable, feature = "tracing"))]
                resource_span: ptr::read(&me.resource_span),
                permits_acquired: me.permits_acquired,
                lock: ptr::read(&me.lock),
                data: me.data,
            }
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/rwlock/owned_write_guard_mapped.rs: 
unsafe { &*self.data } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/rwlock/owned_write_guard_mapped.rs: 
unsafe { &mut *self.data } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/rwlock/read_guard.rs: 
unsafe { std::ptr::read(&me.resource_span) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/rwlock/read_guard.rs: 
unsafe { &*self.data } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/rwlock/write_guard.rs: 
unsafe { std::ptr::read(&me.resource_span) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/rwlock/write_guard.rs: 
unsafe { &*self.data } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/rwlock/write_guard.rs: 
unsafe { &mut *self.data } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/rwlock/write_guard_mapped.rs: 
unsafe { std::ptr::read(&me.resource_span) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/rwlock/write_guard_mapped.rs: 
unsafe { &*self.data } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/rwlock/write_guard_mapped.rs: 
unsafe { &mut *self.data } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/task/atomic_waker.rs: 
unsafe { self.waker.with_mut(|t| (*t).take()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/tests/atomic_waker.rs: 
unsafe { Waker::from_raw(RawWaker::new(ptr::null(), &PANICKING_VTABLE)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/tests/atomic_waker.rs: 
unsafe { Waker::from_raw(RawWaker::new(ptr::null(), &NONPANICKING_VTABLE)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/tests/loom_atomic_waker.rs: 
unsafe { Waker::from_raw(RawWaker::new(ptr::null(), &PANICKING_VTABLE)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/sync/tests/notify.rs: 
unsafe { Waker::from_raw(RawWaker::new(Arc::into_raw(notify2) as *const _, VTABLE)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/task/local.rs: 
unsafe { handle.spawn_local(task, id) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/task/local.rs: 
unsafe {
            // Safety: because the `LocalSet` itself is `!Send`, we know we are
            // on the same thread if we have access to the `LocalSet`, and can
            // therefore access the local run queue.
            self.context.shared.local_state.assert_owner(task)
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/task/local.rs: 
unsafe {
            // Safety: because the `LocalSet` itself is `!Send`, we know we are
            // on the same thread if we have access to the `LocalSet`, and can
            // therefore access the local run queue.
            self.context.shared.local_state.task_pop_front()
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/task/local.rs: 
unsafe { self.context.shared.local_state.owned_is_empty() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/task/local.rs: 
unsafe {
                // Safety: called from the thread that owns `LocalSet`
                self.context.shared.local_state.close_and_shutdown_all();
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/task/local.rs: 
unsafe {
                // Safety: called from the thread that owns `LocalSet`
                self.context.shared.local_state.take_local_queue()
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/task/local.rs: 
unsafe { self.context.shared.local_state.owned_is_empty() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/task/local.rs: 
unsafe {
                    // Safety: if the current `LocalSet` context points to this
                    // `LocalSet`, then we are on the thread that owns it.
                    cx.shared.local_state.task_push_back(task);
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/task/local.rs: 
unsafe {
                        // Safety: we just checked that the thread ID matches
                        // the localset's owner, so this is safe.
                        self.local_state.task_push_back(task);
                    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/task/local.rs: 
unsafe { self.local_state.task_remove(task) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/task/local.rs: 
unsafe { cx.shared.local_state.close_and_shutdown_all(); } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/task/local.rs: 
unsafe { local.context.shared.local_state.task_pop_front() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/atomic_cell.rs: 
unsafe { Box::from_raw(val) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/idle_notified_set.rs: 
unsafe {
            *ptr = List::Idle;
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/idle_notified_set.rs: 
unsafe {
            *ptr = List::Idle;
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/idle_notified_set.rs: 
unsafe { &*prev.as_ptr() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/idle_notified_set.rs: 
unsafe { &mut *ptr } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/idle_notified_set.rs: 
unsafe { (self.func)(ManuallyDrop::take(&mut *ptr)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/idle_notified_set.rs: 
unsafe {
                // Safety: We are holding the lock and `all_entries` is a new
                // LinkedList.
                move_to_new_list(&mut lock.idle, &mut all_entries.all_entries);
                move_to_new_list(&mut lock.notified, &mut all_entries.all_entries);
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/idle_notified_set.rs: 
unsafe {
                let old_my_list = *ptr;
                *ptr = List::Neither;
                old_my_list
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/idle_notified_set.rs: 
unsafe {
                // Safety: We just checked that the entry is in this particular
                // list.
                list.remove(ListEntry::as_raw(&self.entry)).unwrap();
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/idle_notified_set.rs: 
unsafe { ManuallyDrop::take(&mut *ptr) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/idle_notified_set.rs: 
unsafe { func(&mut *ptr, &mut context) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/idle_notified_set.rs: 
unsafe {
            let old_my_list = *ptr;
            if old_my_list == List::Idle {
                *ptr = List::Notified;
            }
            old_my_list
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/idle_notified_set.rs: 
unsafe {
                // Safety: We just checked that we are in this particular list.
                lock.idle.remove(ListEntry::as_raw(me)).unwrap()
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/idle_notified_set.rs: 
unsafe { NonNull::new_unchecked(ptr as *mut ListEntry<T>) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
            L::pointers(ptr).as_mut().set_next(self.head);
            L::pointers(ptr).as_mut().set_prev(None);

            if let Some(head) = self.head {
                L::pointers(head).as_mut().set_prev(Some(ptr));
            }

            self.head = Some(ptr);

            if self.tail.is_none() {
                self.tail = Some(ptr);
            }
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
            let head = self.head?;
            self.head = L::pointers(head).as_ref().get_next();

            if let Some(new_head) = L::pointers(head).as_ref().get_next() {
                L::pointers(new_head).as_mut().set_prev(None);
            } else {
                self.tail = None;
            }

            L::pointers(head).as_mut().set_prev(None);
            L::pointers(head).as_mut().set_next(None);

            Some(L::from_raw(head))
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
            let last = self.tail?;
            self.tail = L::pointers(last).as_ref().get_prev();

            if let Some(prev) = L::pointers(last).as_ref().get_prev() {
                L::pointers(prev).as_mut().set_next(None);
            } else {
                self.head = None;
            }

            L::pointers(last).as_mut().set_prev(None);
            L::pointers(last).as_mut().set_next(None);

            Some(L::from_raw(last))
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe { Some(&*tail.as_ptr()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe { T::pointers(curr).as_ref() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe { &mut *curr.as_ptr() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe { self.list.remove(curr) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
                    let handle = ManuallyDrop::new(T::from_raw(curr));
                    f(&handle);
                    next = T::pointers(curr).as_ref().get_next();
                } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
                if let Some(head) = self.head {
                    debug_assert!(L::pointers(head).as_ref().get_prev().is_none());
                    L::pointers(head).as_mut().set_prev(Some(guard));
                    L::pointers(guard).as_mut().set_next(Some(head));

                    // The list is not empty, so the tail cannot be `None`.
                    let tail = self.tail.unwrap();
                    debug_assert!(L::pointers(tail).as_ref().get_next().is_none());
                    L::pointers(tail).as_mut().set_next(Some(guard));
                    L::pointers(guard).as_mut().set_prev(Some(tail));
                } else {
                    // The list is empty.
                    L::pointers(guard).as_mut().set_prev(Some(guard));
                    L::pointers(guard).as_mut().set_next(Some(guard));
                }
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
                L::pointers(self.guard).as_ref().get_prev().unwrap()
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
                let last = self.tail()?;
                let before_last = L::pointers(last).as_ref().get_prev().unwrap();

                L::pointers(self.guard).as_mut().set_prev(Some(before_last));
                L::pointers(before_last).as_mut().set_next(Some(self.guard));

                L::pointers(last).as_mut().set_prev(None);
                L::pointers(last).as_mut().set_next(None);

                Some(L::from_raw(last))
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe { ptr::addr_of!((*self.inner.get()).prev).read() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe { ptr::addr_of!((*self.inner.get()).next).read() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
            ptr::addr_of_mut!((*self.inner.get()).prev).write(value);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
            ptr::addr_of_mut!((*self.inner.get()).next).write(value);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
            // Remove first
            let mut list = LinkedList::new();

            push_all(&mut list, &[c.as_ref(), b.as_ref(), a.as_ref()]);
            assert!(list.remove(ptr(&a)).is_some());
            assert_clean!(a);
            // `a` should be no longer there and can't be removed twice
            assert!(list.remove(ptr(&a)).is_none());
            assert!(!list.is_empty());

            assert!(list.remove(ptr(&b)).is_some());
            assert_clean!(b);
            // `b` should be no longer there and can't be removed twice
            assert!(list.remove(ptr(&b)).is_none());
            assert!(!list.is_empty());

            assert!(list.remove(ptr(&c)).is_some());
            assert_clean!(c);
            // `b` should be no longer there and can't be removed twice
            assert!(list.remove(ptr(&c)).is_none());
            assert!(list.is_empty());
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
            // Remove middle
            let mut list = LinkedList::new();

            push_all(&mut list, &[c.as_ref(), b.as_ref(), a.as_ref()]);

            assert!(list.remove(ptr(&a)).is_some());
            assert_clean!(a);

            assert_ptr_eq!(b, list.head);
            assert_ptr_eq!(c, b.pointers.get_next());
            assert_ptr_eq!(b, c.pointers.get_prev());

            let items = collect_list(&mut list);
            assert_eq!([31, 7].to_vec(), items);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
            // Remove middle
            let mut list = LinkedList::new();

            push_all(&mut list, &[c.as_ref(), b.as_ref(), a.as_ref()]);

            assert!(list.remove(ptr(&b)).is_some());
            assert_clean!(b);

            assert_ptr_eq!(c, a.pointers.get_next());
            assert_ptr_eq!(a, c.pointers.get_prev());

            let items = collect_list(&mut list);
            assert_eq!([31, 5].to_vec(), items);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
            // Remove last
            // Remove middle
            let mut list = LinkedList::new();

            push_all(&mut list, &[c.as_ref(), b.as_ref(), a.as_ref()]);

            assert!(list.remove(ptr(&c)).is_some());
            assert_clean!(c);

            assert!(b.pointers.get_next().is_none());
            assert_ptr_eq!(b, list.tail);

            let items = collect_list(&mut list);
            assert_eq!([7, 5].to_vec(), items);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
            // Remove first of two
            let mut list = LinkedList::new();

            push_all(&mut list, &[b.as_ref(), a.as_ref()]);

            assert!(list.remove(ptr(&a)).is_some());

            assert_clean!(a);

            // a should be no longer there and can't be removed twice
            assert!(list.remove(ptr(&a)).is_none());

            assert_ptr_eq!(b, list.head);
            assert_ptr_eq!(b, list.tail);

            assert!(b.pointers.get_next().is_none());
            assert!(b.pointers.get_prev().is_none());

            let items = collect_list(&mut list);
            assert_eq!([7].to_vec(), items);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
            // Remove last of two
            let mut list = LinkedList::new();

            push_all(&mut list, &[b.as_ref(), a.as_ref()]);

            assert!(list.remove(ptr(&b)).is_some());

            assert_clean!(b);

            assert_ptr_eq!(a, list.head);
            assert_ptr_eq!(a, list.tail);

            assert!(a.pointers.get_next().is_none());
            assert!(a.pointers.get_prev().is_none());

            let items = collect_list(&mut list);
            assert_eq!([5].to_vec(), items);
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
            // Remove last item
            let mut list = LinkedList::new();

            push_all(&mut list, &[a.as_ref()]);

            assert!(list.remove(ptr(&a)).is_some());
            assert_clean!(a);

            assert!(list.head.is_none());
            assert!(list.tail.is_none());
            let items = collect_list(&mut list);
            assert!(items.is_empty());
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
            // Remove missing
            let mut list = LinkedList::<&Entry, <&Entry as Link>::Target>::new();

            list.push_front(b.as_ref());
            list.push_front(a.as_ref());

            assert!(list.remove(ptr(&c)).is_none());
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/linked_list.rs: 
unsafe {
                        let entry = ll.remove(ptr(&entries[expect as usize])).unwrap();
                        assert_eq!(expect, entry.val);
                    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/memchr.rs: 
unsafe { libc::memchr(start.cast(), needle as _, haystack.len()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/once_cell.rs: 
unsafe { &*(self.value.get() as *const T) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/once_cell.rs: 
unsafe {
                std::ptr::write(value_ptr, set_to);
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/once_cell.rs: 
unsafe {
                std::ptr::drop_in_place(value_ptr);
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/ptr_expose.rs: 
unsafe { std::mem::transmute(ptr) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/ptr_expose.rs: 
unsafe { maybe_ptr.unwrap_unchecked() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/ptr_expose.rs: 
unsafe { std::mem::transmute(_ptr) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/ptr_expose.rs: 
unsafe { maybe_ptr.unwrap_unchecked() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/rc_cell.rs: 
unsafe { self.with_inner(|rc| rc.clone()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/rc_cell.rs: 
unsafe { self.with_inner(|rc| std::mem::replace(rc, val)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/sharded_list.rs: 
unsafe { lock.remove(node) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/sharded_list.rs: 
unsafe { L::get_shard_id(L::as_raw(val)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/sharded_list.rs: 
unsafe { self.lists.get_unchecked(id & self.shard_mask).lock() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/sharded_list.rs: 
unsafe { L::get_shard_id(L::as_raw(&val)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/try_lock.rs: 
unsafe { &*self.lock.data.get() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/try_lock.rs: 
unsafe { &mut *self.lock.data.get() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/wake.rs: 
unsafe { Waker::from_raw(RawWaker::new(ptr, waker_vtable::<W>())) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/wake_list.rs: 
unsafe { self.end.offset_from(self.start) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/wake_list.rs: 
unsafe { ptr::drop_in_place(slice) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/wake_list.rs: 
unsafe { start.add(self.curr) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/wake_list.rs: 
unsafe { ptr::read(guard.start) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/wake_list.rs: 
unsafe { guard.start.add(1) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/src/util/wake_list.rs: 
unsafe { ptr::drop_in_place(slice) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/fs_file.rs: 
unsafe { File::from_raw_fd(raw_fd) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_async_fd.rs: 
unsafe {
        let res = libc::send(
            stream.as_raw_fd(),
            data.as_ptr().cast(),
            data.len(),
            libc::MSG_OOB,
        );
        if res == -1 {
            Err(io::Error::last_os_error())
        } else {
            Ok(res as usize)
        }
    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_async_fd.rs: 
unsafe {
        libc::setsockopt(
            udp_socket.as_raw_fd(),
            libc::SOL_SOCKET,
            libc::SO_TIMESTAMP,
            &options as *const _ as *const libc::c_void,
            std::mem::size_of_val(&options) as libc::socklen_t,
        )
    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_async_fd.rs: 
unsafe {
        let res = libc::setsockopt(
            socket.as_raw_fd(),
            libc::SOL_IP,
            libc::IP_RECVERR,
            &recv_err as *const _ as *const libc::c_void,
            std::mem::size_of_val(&recv_err) as libc::socklen_t,
        );
        if res == -1 {
            panic!("{:?}", std::io::Error::last_os_error());
        }
    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_async_fd.rs: 
unsafe { std::mem::MaybeUninit::<libc::sockaddr_in>::zeroed().assume_init() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_async_fd.rs: 
unsafe { std::mem::MaybeUninit::zeroed().assume_init() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_async_fd.rs: 
unsafe { libc::sendmsg(socket_fd, &msg, 0) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_buf_reader.rs: 
unsafe { &mut *this } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe { self.map_unchecked_mut(|s| &mut s.0 .0) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe { self.map_unchecked_mut(|s| &mut s.0 .0) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe {
                let p = self.0.as_mut().get_unchecked_mut();
                libc::aio_fsync(libc::O_SYNC, p)
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe { mem::MaybeUninit::zeroed().assume_init() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe {
                self.0.aio_sigevent = mem::zeroed();
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe { self.map_unchecked_mut(|s| &mut *(s.0)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe { libc::aio_return(self.0 .0.as_mut().get_unchecked_mut()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe { mem::MaybeUninit::zeroed().assume_init() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe {
            let p = poll_aio.0.as_mut().get_unchecked_mut();
            libc::aio_fsync(libc::O_SYNC, p)
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe { mem::MaybeUninit::zeroed().assume_init() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe { mem::zeroed() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe { self.aiocb.as_mut().get_unchecked_mut() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe { libc::lio_listio(libc::LIO_NOWAIT, p, 1, &mut self.sev) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe { mem::MaybeUninit::zeroed().assume_init() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe {
                self.sev = mem::zeroed();
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe { self.map_unchecked_mut(|s| &mut *(s.0)) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe {
                        let p1 = self.get_unchecked_mut();
                        let p2: &mut [&mut libc::aiocb; 1] =
                            p1.0.aiocb.as_mut().get_unchecked_mut();
                        let p3: &mut libc::aiocb = p2[0];
                        libc::aio_return(p3)
                    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe { mem::zeroed() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_poll_aio.rs: 
unsafe { mem::zeroed() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_read.rs: 
unsafe { self.leaked_buffers.create(buf.capacity()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_read_buf.rs: 
unsafe {
        read_buf.advance_mut(1);
    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_read_buf.rs: 
unsafe {
        read_buf.advance_mut(5);
    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_read_buf.rs: 
unsafe {
        read_buf.advance_mut(2);
    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_read_buf.rs: 
unsafe {
        read_buf.advance_mut(8);
    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_read_buf.rs: 
unsafe {
        uninit.advance_mut(4);
    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/io_take.rs: 
unsafe { self.leaked_buffers.create(10) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/process_raw_handle.rs: 
unsafe { GetProcessId(handle as _) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/uds_cred.rs: 
unsafe { geteuid() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/uds_cred.rs: 
unsafe { getegid() } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/support/io_vec.rs: 
unsafe { slice::from_raw_parts(tail.as_ptr(), tail.len()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio/tests/support/signal.rs: 
unsafe {
        assert_eq!(kill(getpid(), signal), 0);
    } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-test/src/task.rs: 
unsafe {
            let raw = to_raw(self.waker.clone());
            Waker::from_raw(raw)
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/either.rs: 
unsafe {
            match $self.get_unchecked_mut() {
                Self::Left(l) => Pin::new_unchecked(l).$method($($args),+),
                Self::Right(r) => Pin::new_unchecked(r).$method($($args),+),
            }
        } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/io/stream_reader.rs: 
unsafe { Pin::into_inner_unchecked(self) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/io/stream_reader.rs: 
unsafe { Pin::new_unchecked(&mut me.inner) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/sync/cancellation_token.rs: 
unsafe { Self::new_future(&cancellation_token) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/sync/cancellation_token.rs: 
unsafe {
                Self::new_future(this.cancellation_token)
            } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/sync/mpsc.rs: 
unsafe { mem::transmute::<InnerFuture<'_, T>, InnerFuture<'static, T>>(v) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/sync/mpsc.rs: 
unsafe { &mut *inner } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/sync/reusable_box.rs: 
unsafe { Pin::into_inner_unchecked(boxed) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/sync/reusable_box.rs: 
unsafe { raw.write(new_value) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/sync/reusable_box.rs: 
unsafe { Box::from_raw(raw) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/sync/reusable_box.rs: 
unsafe { ptr::drop_in_place(raw) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/sync/reusable_box.rs: 
unsafe { ManuallyDrop::take(&mut this.f) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/sync/reusable_box.rs: 
unsafe { ManuallyDrop::take(&mut self.f) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/udp/frame.rs: 
unsafe { &mut *(pin.rd.chunk_mut() as *mut _ as *mut [MaybeUninit<u8>]) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/udp/frame.rs: 
unsafe { pin.rd.advance_mut(read.filled().len()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/util/maybe_dangling.rs: 
unsafe { core::ptr::drop_in_place(self.0.as_mut_ptr()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/util/maybe_dangling.rs: 
unsafe { self.map_unchecked_mut(|this| this.0.assume_init_mut()) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/util/poll_buf.rs: 
unsafe { &mut *(dst as *mut _ as *mut [MaybeUninit<u8>]) } 
 
En archivo ../proyectos_para_analisar/tokio-master/tokio-util/src/util/poll_buf.rs: 
unsafe {
        buf.advance_mut(n);
    } 
 
